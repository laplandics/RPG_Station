using System.Collections.Generic;
using UnityEngine;

public class TileTransitionsGetter
{
    public int GetTileTransitionIndex(Tile tile, Dictionary<Vector2Int, Tile> tiles)
    {
        var tilePos = tile.Position;
        return -1;
    }
}